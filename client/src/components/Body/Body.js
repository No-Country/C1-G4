import React, { Fragment } from "react";
import styles from "../../styles/Body.module.css";

function Body() {
  return (
    <Fragment>
      <div className={ styles.body }>
        {/* user = {user} */}
      </div>
    </Fragment>
  );
}
export default Body;
